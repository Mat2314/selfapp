import { InterceptedSrcDirective } from './intercepted-src.directive';

describe('InterceptedSrcDirective', () => {
  it('should create an instance', () => {
    const directive = new InterceptedSrcDirective();
    expect(directive).toBeTruthy();
  });
});
